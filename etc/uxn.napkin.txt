Memory

	A byte' is 8 bits
	A short" is two big-endian bytes

	There are 2 stacks
	A stack has 256 bytes of data
	A stack pointer is a byte and wraps

	Dev is 256 bytes and wraps
	Ram is 65536 bytes and wraps
	Rom is loaded and evaluated at 256

Bytecode

	20 Values are shorts instead of bytes
	40 Values are read or written on . the other stack
	80 Values are not consumed when read
	[] moves pc by signed' or to absolute"

	          a  01  a+1
	          a  02
	        a b  03  b
	        a b  04  b a
	      a b c  05  b c a
	          a  06  a a
	        a b  07  a b a
	        a b  08  a=b'
	        a b  09  a≠b'
	        a b  0a  a>b'
	        a b  0b  a<b'
	       addr [0c]
	 cond' addr [0d]
	       addr [0e] . pc"
	          a  0f  . a
	       abs'  10  #abs
	  val #abs'  11
	       sig'  12  #sig
	  val #sig'  13
	       abs"  14  #abs
	  val #abs"  15
	       dev'  16  ?dev
	  val ?dev'  17
	        a b  18  a+b
	        a b  19  a-b
	        a b  1a  a×b
	        a b  1b  a÷b|0
	        a b  1c  a&b
	        a b  1d  a|b
	        a b  1e  a^b
	      a hl'  1f  a>>l<<h

	Immediate opcodes have no modes
	[] moves pc by #pc"
	
	             00  return evaluation
	      cond' [20]
	            [40]
	            [60] . pc"
	             80  #pc'
	             a0  #pc"
	             c0  . #pc'
	             e0  . #pc"
