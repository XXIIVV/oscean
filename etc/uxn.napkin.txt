Memory

	A byte' is 8 bits
	A short" is two big-endian bytes

	There are 2 stacks
	Each stack has 256 bytes of data
	Each stack pointer is a byte and wraps

	Dev(D) is 256 bytes and wraps
	Ram(M) is 65536 bytes and wraps
	Rom is loaded and evaluated at 256

Bytecode

	20|opcode Values are shorts instead of bytes
	40|opcode Values are read or written on . the other stack
	80|opcode Values are not consumed when read
	{} moves pc by signed-addr' or to absolute-addr"

	           a  01  a+1
	           a  02
	         a b  03  b
	         a b  04  b a
	       a b c  05  b c a
	           a  06  a a
	         a b  07  a b a
	         a b  08  a=b'
	         a b  09  a≠b'
	         a b  0a  a>b'
	         a b  0b  a<b'
	        addr {0c}
	  cond' addr {0d}
	        addr {0e} . pc"
	           a  0f  . a
	        abs'  10  M[abs]
	 val M[abs]'  11
	        sig'  12  M[sig]
	 val M[sig]'  13
	        abs"  14  M[abs]
	 val M[abs]"  15
	        abs'  16  D[abs]
	 val D[abs]'  17
	         a b  18  a+b
	         a b  19  a-b
	         a b  1a  a×b
	         a b  1b  a÷b|0
	         a b  1c  a&b
	         a b  1d  a|b
	         a b  1e  a^b
	       a hl'  1f  a>>l<<h

	Immediate opcodes have no modes
	{} moves pc by M[pc]"
	
	              00  return evaluation
	       cond' {20}
	             {40}
	             {60} . pc"
	              80  M[pc]'
	              a0  M[pc]"
	              c0  . M[pc]'
	              e0  . M[pc]"
